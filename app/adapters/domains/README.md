# Domain adapters

Add one domain profile, a `DomainAdapter` implementation, and an import script here. The API,
`TradePilotState`, repository protocols, and main LangGraph flow must remain unchanged.
